package com.example.amovtp.ui.viewmodels.AddViewModels

import androidx.lifecycle.ViewModel

class AddLocationViewModel : ViewModel() {

    //TODO: ir buscar informação ao GeoInfoRepository

    //TODO: ajustar as funções consoante a informação recebida
    /*fun addLocation(
        locationName: String,
        locationDescription: String,
        latitude: Double,
        longitude: Double
        //TODO: recebe pelo menos uma imagem
    ): Boolean {

        // checks if the location already exists
        for (i in _locations) {
            if (i.name == locationName)
                return false
        }

        // add location
        _locations.add(
            Location(
                _currentLocationId++,
                locationName,
                locationDescription,
                latitude,
                longitude
            )
        )

        return true

    }*/

}